spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:item;MODE=MYSQL;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    hibernate:
      ddl-auto: create-drop
