spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:item;MODE=MYSQL
    username: sa
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true